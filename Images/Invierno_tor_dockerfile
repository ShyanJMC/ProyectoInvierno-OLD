####File for generate the Tor relay
#
#ARM password; ProyectoInvierno

FROM debian:latest
MAINTAINER ShyanJMC (nothing_to_do@domain.com)

RUN apt update && apt upgrade -y
RUN apt install tor tor-arm tor-geoipdb procps -y 

##### Update;
#Corregir para que Tor tenga el hash configurado
RUN echo "apt update && apt upgrade -y && arm" >> /root/.bashrc
